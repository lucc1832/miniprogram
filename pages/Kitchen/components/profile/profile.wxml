<view class="profile-container" style="padding-top: {{statusBarHeight + navBarHeight}}px;">
  <!-- Banner -->
  <view class="banner">
    <view class="banner-text">
      <text class="banner-title">Food, Family, Love</text>
      <text class="banner-subtitle">唯有爱与美食不可辜负</text>
    </view>
    <view class="banner-img">
      <!-- Placeholder for food illustration -->
      <text style="font-size: 80rpx;">🍱</text>
    </view>
    <view class="video-tutorial" bindtap="showTutorial">
      <text>📹 视频教程</text>
    </view>
  </view>

  <!-- Function Grid -->
  <view class="func-grid">
    <view class="func-item" wx:for="{{funcs}}" wx:key="name" bindtap="handleFuncClick" data-action="{{item.action}}">
      <view class="func-icon" style="background: {{item.color}}">{{item.icon}}</view>
      <text class="func-name">{{item.name}}</text>
    </view>
  </view>

  <!-- Info Modal -->
  <view class="modal-mask" wx:if="{{showModal}}" bindtap="closeModal">
    <view class="modal-content" catchtap="true">
      <view class="modal-header">
        <text class="modal-title">{{modalTitle}}</text>
        <text class="close-btn" bindtap="closeModal">×</text>
      </view>
      <text class="modal-text">{{modalContent}}</text>
    </view>
  </view>

  <!-- Ingredients List Modal -->
  <view class="modal-mask" wx:if="{{showIngredientList}}" bindtap="closeIngList">
    <view class="modal-content" catchtap="true">
      <view class="modal-header">
        <text class="modal-title">我的食材</text>
        <text class="close-btn" bindtap="closeIngList">×</text>
      </view>
      <scroll-view scroll-y style="max-height: 400rpx;">
        <view wx:for="{{ingredients}}" wx:key="*this" style="padding: 10rpx 0; border-bottom: 1rpx solid #eee;">
          {{item}}
        </view>
      </scroll-view>
    </view>
  </view>
</view>